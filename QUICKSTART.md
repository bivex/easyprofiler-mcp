# Quick Start Guide

## Тестирование парсера на вашем файле

Ваш файл: `C:\Users\Admin\Desktop\Dev\easy_profiler\easy_profiler-v2.1.0-msvc15-win64\bin\test.prof` (160MB)

### Успешно распарсили!

✅ **Результаты:**
- **Версия формата:** 0x2010000 (v2.1.0)
- **PID:** 7220
- **Длительность профилирования:** 11.14 ms
- **Потоков:** 7
- **Блоков:** 6,037,606
- **Дескрипторов:** 27
- **Время парсинга:** ~48 секунд
- **Использование памяти:** ~507 MB

### Топ потоков по количеству блоков:

1. **Main Thread** (18700): 2,265,001 блоков, 89 context switches
2. **Modelling** (9176): 2,265,001 блоков, 55 context switches
3. **Render** (15064): 1,507,501 блоков, 125 context switches
4. **Resource loading** (19016): 101 блоков, 4 context switches
5. **EasyProfiler.ETW** (1256): 1 блок, 17 context switches

## Использование MCP сервера

### 1. Сборка (если еще не сделано)

```bash
cd easyprofiler-mcp
go build -o easyprofiler-mcp.exe
```

### 2. Конфигурация Claude Desktop

Добавьте в `%APPDATA%\Claude\claude_desktop_config.json`:

```json
{
  "mcpServers": {
    "easyprofiler": {
      "command": "C:\\Users\\Admin\\Desktop\\Dev\\easy_profiler\\easyprofiler-mcp\\easyprofiler-mcp.exe"
    }
  }
}
```

### 3. Перезапустите Claude Desktop

### 4. Примеры запросов

#### Загрузка профиля

```
Загрузи профиль из файла C:\Users\Admin\Desktop\Dev\easy_profiler\easy_profiler-v2.1.0-msvc15-win64\bin\test.prof с fast_mode=false
```

**Ожидаемый ответ:**
```json
{
  "status": "success",
  "file": "C:\\Users\\Admin\\Desktop\\...\\test.prof",
  "fast_mode": false,
  "version": "0x2010000",
  "pid": 7220,
  "total_duration": "11.138841ms",
  "threads_count": 7,
  "blocks_count": 6037606,
  "descriptors_count": 27,
  "bookmarks_count": 0,
  "memory_mb": "145.55"
}
```

#### Анализ проблем производительности

```
Проанализируй проблемы производительности
```

Это выявит:
- Долгие блокировки (>100ms)
- Горячие функции (>10% времени)
- Дисбаланс потоков
- Избыточные переключения контекста

#### Топ самых медленных блоков

```
Покажи топ 20 самых медленных блоков
```

#### Статистика по потокам

```
Покажи статистику использования времени по потокам
```

Ожидается увидеть:
- Main Thread: ~37.5% времени
- Modelling: ~37.5% времени
- Render: ~25% времени

#### Горячие точки

```
Найди функции с наибольшим суммарным временем выполнения
```

## Рекомендации для вашего профиля

### Потенциальные проблемы

1. **Много блоков (6M+)**
   - Может указывать на слишком детальное профилирование
   - Рассмотрите использование более крупных блоков
   - Или профилируйте только критичные секции

2. **Main и Modelling потоки равны**
   - Оба потока имеют одинаковое количество блоков (2.26M)
   - Возможно дублирование измерений
   - Или синхронная работа потоков

3. **Короткое время профилирования (11ms)**
   - При таком количестве блоков за 11ms
   - Средняя длительность блока: ~1.8 наносекунд
   - Это ОЧЕНЬ быстро - возможно проблема с временными метками

### Что проверить

```
Загрузи профиль и найди самые медленные блоки
```

Посмотрите:
- Какие функции реально занимают время
- Есть ли блоки длительнее 1ms
- Корректны ли временные метки

```
Покажи горячие точки с лимитом 30
```

Это покажет какие функции вызываются чаще всего.

## Оптимизация для больших файлов

Для файлов >100MB используйте fast mode:

```
Загрузи профиль C:\...\test.prof с fast_mode=true
```

**Fast mode:**
- ✅ Пропускает context switches (экономит немного памяти)
- ✅ Пропускает bookmarks
- ⚠️ Пока не влияет на скорость парсинга (в разработке)

### Будущие оптимизации (v1.1.0)

Планируется:
- Сэмплинг блоков (читать каждый 10-й)
- Ограничение глубины вложенности
- Streaming анализ без полной загрузки
- Multi-threaded парсинг
- Прогресс-бар при загрузке

## Troubleshooting

### "Failed to parse profile: unexpected EOF"

✅ **ИСПРАВЛЕНО в v1.0.1!**

Эта ошибка возникала из-за неправильного определения конца секции потоков. Теперь парсер:
- Корректно читает thread_count из заголовка (v2.1.0+)
- Правильно определяет конец секции по сигнатуре
- Различает uint32 и uint64 thread_id по версии

### Медленный парсинг

**Текущая производительность:**
- 160MB файл → 48 секунд
- ~6M блоков → ~125K блоков/сек

**Это нормально** для первой версии. Планируются оптимизации:
1. Использование буферизованного чтения
2. Пул горутин для парсинга потоков
3. Оптимизация аллокаций памяти

### Много памяти (507MB для 160MB файла)

Это ожидаемо, потому что:
- Бинарный формат → распакованные структуры Go
- 6M блоков × ~84 байта/блок ≈ 504 MB
- Плюс дескрипторы, строки, метаданные

**Для уменьшения:**
- Используйте fast_mode (пропускает CS)
- Ждите v1.1.0 с сэмплингом блоков

## Следующие шаги

1. ✅ Парсер работает с вашим файлом
2. ✅ Можно анализировать производительность
3. ⏳ Рассмотрите результаты анализа
4. 💡 Оптимизируйте код на основе горячих точек
5. 🔄 Перепрофилируйте и сравните

## Вопросы?

Создайте issue в репозитории или опишите проблему в чате.

## Полезные команды

### Быстрый анализ
```
Загрузи профиль test.prof и проанализируй проблемы производительности
```

### Детальный анализ
```
1. Загрузи профиль test.prof
2. Покажи топ 50 самых медленных блоков
3. Покажи статистику по потокам
4. Найди горячие точки (топ 30)
5. Проанализируй проблемы
```

### Сравнение потоков
```
Загрузи профиль и покажи какой поток работает дольше всего
```

---

**Версия:** 1.0.1
**Дата:** 2025-11-09
**Статус:** ✅ Протестировано на реальном 160MB файле
